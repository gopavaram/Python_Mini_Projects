Power Query (PQ) -> Power Pivote (PP) (DAX) -> Power BI (Viz)

Data source -> Power Query (Performe transformation)  -> DAX (Calculated Columns, Measures) -> Dashboarding

**A measure should produce a single value (Valid Measure)
